-- 유저와 상품을 그룹화, 그 때 COUNT한 수가 2개 이상인 경우 SELECT

SELECT
    USER_ID,
    PRODUCT_ID
FROM
    ONLINE_SALE
GROUP BY
    USER_ID,
    PRODUCT_ID
HAVING
    COUNT(*) >= 2
ORDER BY
    USER_ID ASC,
    PRODUCT_ID DESC;
